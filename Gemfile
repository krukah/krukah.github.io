source "https://rubygems.org"

# Local preview only. GitHub Pages builds this site with its own Jekyll,
# and the site uses no plugins, so nothing here needs to match production.
gem "jekyll", "~> 4.4"
gem "webrick"
